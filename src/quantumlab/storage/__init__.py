"""Подсистемы хранения (§24 ТЗ).

Разделение данных:

* **метаданные** (задания, проекты, пользователи) — SQL; в этом слое пока
  файловое хранилище :class:`LocalJobStore` как референс для локального режима;
* **большие научные данные** (градиенты, орбитали, кубические сетки) — объектное
  хранилище, ссылки в виде ``artifact://…``;
* **кэш** — Redis или аналог.
"""

from quantumlab.storage.local_jobs import LocalJobStore

__all__ = ["LocalJobStore"]
