"""REST API QuantumLab (контракт — ``api/openapi/v1.yaml``).

Третий фронтенд поверх того же бэкенда, что и CLI: доменная модель, реестр
возможностей, автоподбор параметров и расчётное ядро общие. Ничего из научной
логики здесь не живёт — сервер только переводит HTTP в вызовы домена и обратно.

Эндпоинты, реализация которых ещё не готова, возвращают ``501`` с локализованным
``Problem`` (RFC 9457), а не ``404``: путь объявлен в контракте, и притворяться,
что его не существует, — значит скрывать состояние проекта (§54 ТЗ).
"""

from __future__ import annotations

from quantumlab.server.app import create_app

__all__ = ["create_app"]
