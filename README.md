# QuantumLab

**Профессиональная квантовая химия, которой можно пользоваться без изучения
языка input-файлов.**

Сложность находится внутри системы, а не на плечах пользователя: молекула →
тип расчёта → уровень точности → «Рассчитать». При этом эксперт никогда не
упирается в потолок интерфейса — полный контроль доступен через расширенные
настройки, CLI и API.

> **Статус проекта: Этап 2 (Compute core) — частично.** Реализован контрактный
> слой платформы (Этап 1) и **референсное расчётное ядро**: базисные наборы,
> интегралы по McMurchie–Davidson, RHF с DIIS и движок `ReferenceEngine`,
> доступный из CLI как настоящий расчёт. Энергии сверены с PySCF как
> независимым оракулом — расхождение ≤ 1e-6 Eh на пяти базисах.
> Умеет пока одно: **энергию RHF в одной точке** (`single_point`).
> DFT, градиенты, оптимизация геометрии, Web UI и планировщики **не реализованы**
> — система сообщает об этом прямо, а не имитирует расчёт.
> Проверить: `quantumlab capabilities`.

---

## Быстрый старт

```bash
python3 -m venv .venv && source .venv/bin/activate
pip install -e ".[dev]"

quantumlab version
quantumlab molecule inspect tests/fixtures/water.xyz
quantumlab plan tests/fixtures/water.xyz --task optimize --profile high-accuracy

# Настоящий расчёт: энергия RHF в одной точке
quantumlab run tests/fixtures/water.xyz --task energy --method hf --basis sto-3g
quantumlab job list

# Не реализовано — и система говорит об этом прямо:
quantumlab run tests/fixtures/water.xyz --task optimize --profile standard
```

Русский интерфейс — по умолчанию, английский — `--lang en`.

Реальный запуск даёт энергию, орбитали, диполь и проверки качества:

```
Запускаю Одноточечный расчёт методом hf в базисе sto-3g…
  SCF сошёлся за 8 итераций
  Энергия: -74.9630296563 э · ЗМО: -0.391243 э · НСМО: 0.605165 э · Диполь: 1.7253 Д
  Результат сохранён: ~/.quantumlab/results/<id>.json
```

## Что работает сейчас

| Компонент | Статус |
|---|---|
| Доменная модель: молекула, спецификация расчёта, задание, результат | ✅ |
| Автоподбор параметров с объяснением каждого решения | ✅ |
| Локализация ru (по умолчанию) + en, 324 ключа, паритет под тестом | ✅ |
| Человекочитаемые ошибки: «что произошло / что попробовали / что сделать» | ✅ |
| Контракты расчётного ядра (`Protocol`: интегралы, SCF, DFT, корреляция, оптимизация, частоты, backend) | ✅ |
| Реестр возможностей с честными статусами | ✅ |
| Машина состояний задания, атомарное хранилище, повтор с чекпоинта | ✅ |
| Отпечаток воспроизводимости расчёта | ✅ |
| CLI: `version`, `capabilities`, `molecule inspect`, `plan`, `run`, `job …` | ✅ |
| **Движок `ReferenceEngine`**: `single_point` методом RHF, результат с проверками качества | ✅ сверено с PySCF |
| CLI выполняет расчёт по-настоящему и сохраняет JSON-результат | ✅ |
| Контракт REST API (OpenAPI 3.1: 23 пути, 26 операций) | ✅ |
| Базисные наборы: 16 наборов из Basis Set Exchange с записью происхождения | ✅ |
| Интегралы S, T, V, (μν\|λσ), диполь — декартовы гауссианы, McMurchie–Davidson | ✅ сверено с квадратурой до 1e-14 |
| RHF с DIIS, гашением и сдвигом уровней; протокол применённых стратегий | ✅ сверено с PySCF до 1e-6 Eh |
| Сверка с PySCF как независимым оракулом (19 тестов, маркер `scientific`) | ✅ |
| **DFT, MP2, CC, градиенты, оптимизация геометрии, частоты** | ❌ не реализовано |
| **UHF/ROHF, сферические базисы (d/f в чистой угловой схеме)** | ❌ не реализовано |
| **Web UI, REST-сервер, планировщики, GPU, MPI** | ❌ не реализовано |

### Известные ограничения расчётного ядра

* **Только декартова схема.** Для базисов с d/f-функциями (cc-pV\*, aug-cc-pV\*,
  def2-\*) мы используем 6 декартовых d-функций вместо 5 сферических. Это
  корректный, но **больший** базис: энергии отличаются от общепринятых значений
  на ~1e-4 Eh. Данные помечены полем `angular_scheme_published`, статус в
  реестре — `partial`, а не `implemented`.
* **Только RHF.** Нечётное число электронов отклоняется явной ошибкой, а не
  молчаливым неверным результатом.
* Референсная реализация медленная: O(N⁴) без скрининга, один поток, чистый
  Python во внутренних циклах. Это сознательно (ADR-002): сначала корректный
  эталон, потом оптимизация.

## Проверка качества

```bash
make check     # ruff + mypy --strict + pytest
```

На текущем коммите: ruff — чисто, mypy `--strict` — 0 ошибок в 39 файлах,
pytest — 169 тестов.

Быстро проверить само ядро:

```bash
pytest tests/test_engine_reference.py -q          # движок: числа + честность отказов
pytest tests/test_crosscheck_pyscf.py -m scientific   # сверка с PySCF (нужен pyscf)
```

## Документация

| Раздел | Содержание |
|---|---|
| [Архитектура](docs/architecture/01-overview.md) | диаграмма, принципы, границы модулей |
| [Структура репозитория](docs/architecture/02-repository-structure.md) | что где лежит |
| [Технологический стек](docs/architecture/03-technology-stack.md) | выбор и обоснование |
| [Доменная модель и схема БД](docs/architecture/04-domain-model.md) | сущности, состояния, SQL |
| [Контракты API](docs/architecture/05-api-contracts.md) | REST, CLI, Python SDK |
| [Расчётное ядро](docs/architecture/06-quantum-engine.md) | алгоритмы, производительность |
| [UX](docs/architecture/07-ux-flows.md) | мастер, локализация, ошибки |
| [Выполнение и HPC](docs/architecture/08-execution-hpc.md) | очередь, надёжность, восстановление |
| [Верификация и бенчмарки](docs/architecture/09-verification-and-benchmark.md) | методология |
| [План реализации](docs/architecture/10-roadmap.md) | этапы, MVP, DoD |
| [ADR](docs/architecture/adr/) | ключевые решения |
| [Мой первый расчёт за 5 минут](docs/user/quickstart.md) | руководство пользователя (рус.) |
| [Инструкции разработчика](docs/dev/development.md) | стандарты кода, как расширять |
| [Развёртывание](docs/ops/hpc-deployment.md) | Docker, K8s, Slurm |

## Принципы

1. **Один backend на все интерфейсы** — GUI, CLI, REST и SDK вызывают одни и те
   же доменные сервисы.
2. **Честность вместо имитации** — нереализованный метод недоступен для выбора,
   а не «приблизительно считается».
3. **Сначала корректность, потом скорость** — референс → верификация →
   профилирование → оптимизация → бенчмарк.
4. **Всё, что влияет на результат, — данные** — спецификация сериализуема и
   хешируется в отпечаток расчёта.
5. **Локализация — часть контракта** — в коде только ключи переводов.

## Лицензия

Apache-2.0.
